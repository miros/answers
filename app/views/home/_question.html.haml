.question_summary
  .votes.rating
    =question.rating
  .status{:class => (question.answers_count && question.answers_count > 0) ? :answered : :unanswered}
    .counter
      =question.answers_count
    %div
      ответов
  .views
    .counter= question.question_views_count
    просмотров   
  .question_info
    %h3= link_to question.title, question
    .tags
      =render question.tags 
    .creation_info
      %span.created_at= time_ago question.created_at
      %a= question.user.name
.clear


